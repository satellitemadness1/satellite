#pragma once
// satellite-004's version -- the one place it is written.
//
// "satellite 004 revision 02" (the author, 2026-09-15: "let's call it 004 02 from
// now on ... since we have blew past 004 01"). Revision 01 was the first night's
// prototype; 02 is everything since (the word table, satellite_string and its
// libraries). satellite 003 revision 07, archived in old_versions/second_satellite/,
// is a different program and keeps its own number.
//
// VERSION is the language, REVISION is this build of it -- the same two numbers
// 003 keeps (old_versions/second_satellite/src/system_facts/version.hpp).

namespace satellite004 {

inline constexpr const char *kVersion = "004";
inline constexpr const char *kRevision = "02";

} // namespace satellite004
