# PYTHON/M320 — `http.server`, `urllib.request`, `requests`

**Introduced:** Python 1.0 / 3.0. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
urllib.request.urlopen("https://example.com").read()
```

## Why

Follows `socket`. `satellite.network.http(port)` and `https(host, …)` are among the numbered, unbuilt paths.
