# PYTHON/M313 — `threading.Lock` and `RLock`

**Introduced:** Python 1.5. Present in Python 3.12.
**Answer: satellite milestone [M40](../SATELLITE/M40-locks-and-atomics.md).**

## Python

```python
with lock:
    count += 1
```

## Why

[M40](../SATELLITE/M40-locks-and-atomics.md), whose page names `threading.Lock`. Until then, give each thread its own data and combine the answers from `join()`, which is race-free.
