# PYTHON/M261 — `os.mkdir()`, `os.rename()`, `shutil.copy()`, `shutil.rmtree()`

**Introduced:** Python 1.0 / 2.3. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
os.makedirs("out/logs", exist_ok=True)
```

## Why

Not in `satl --words`. **Open, and [CXX23 row 299](../CXX23/M299.md).**
