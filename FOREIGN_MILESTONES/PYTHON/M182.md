# PYTHON/M182 — abstract base classes `abc.ABC`, `@abstractmethod`

**Introduced:** Python 2.6. Present in Python 3.12.
**Answer: satellite milestone [M31](../SATELLITE/M31-user-generics.md).**

## Python

```python
class Shape(ABC):
    @abstractmethod
    def area(self): ...
```

## Why

An interface with no body, the same question as Java's `interface`, which `foreign.cpp` sends to [M31](../SATELLITE/M31-user-generics.md) and the decision after it.
