# PYTHON/M322 — `tkinter`

**Introduced:** Python 1.1. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
root = tkinter.Tk()
```

## Why

`satellite.window` and `satellite.variable.window` are numbered, and `satellite.help` on `satellite.window.new` is `S1101`. Open until built.
