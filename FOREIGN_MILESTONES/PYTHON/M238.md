# PYTHON/M238 — `heapq` and `queue.PriorityQueue`

**Introduced:** Python 2.3. Present in Python 3.12.
**Answer: partial — satellite has part of it.**

## Python

```python
heapq.heappush(h, x)
smallest = heapq.heappop(h)
```

## satellite

```
h.append(x)
h.sort()
satellite.variable.number smallest = h.first()
h.remove_first()
```

## What differs

No heap: sort, then take the front. Correct, and slower per insert.
