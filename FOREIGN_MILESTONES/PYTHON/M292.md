# PYTHON/M292 — `struct`

**Introduced:** Python 1.4. Present in Python 3.12.
**Answer: never.**

## Python

```python
struct.pack("<I", n)
```

## satellite

```
satellite.variable.binary bits = n.binary()
```

## What differs

Byte layout is not visible. `binary` and `hex` are where bit-level values live.
