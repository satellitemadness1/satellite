# PYTHON/M81 — `list`

**Introduced:** Python 1.0. Present in Python 3.12.
**Answer: satellite says it.**

## Python

```python
xs = []
xs.append(3)
```

## satellite

```
satellite.container.list<satellite.variable.number> xs = satellite.container.list()
xs.append(3)
```

## What differs

One list, generic over its element type. A list of mixed types is `list<satellite.variable.variant>`: checked 2026-09-12, `1` and `"two"` in one list displayed `[1, two]`. **Assigning a list copies it**, which Python does not do (row [M198](M198.md)).
