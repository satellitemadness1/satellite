# PYTHON/M240 — set operations `| & - ^`

**Introduced:** Python 2.4. Present in Python 3.12.
**Answer: partial — satellite has part of it.**

## Python

```python
common = a & b
```

## Why

With sets as maps of bools (row [M86](M86.md)), intersection is a loop over one map's keys asking `has(k)` of the other.
