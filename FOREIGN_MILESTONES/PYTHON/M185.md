# PYTHON/M185 — `__add__` and arithmetic dunders

**Introduced:** Python 1.0. Present in Python 3.12.
**Answer: satellite milestone [M38](../SATELLITE/M38-operator-overloading.md).**

## Python

```python
def __add__(self, other): ...
```

## Why

[M38](../SATELLITE/M38-operator-overloading.md), whose page names `__add__`. Only `+` is planned; `__mul__`, `__sub__` and the rest are not.
