# PYTHON/M186 — `__len__`, `__getitem__`, `__contains__`, `__iter__`

**Introduced:** Python 1.0 / 2.2. Present in Python 3.12.
**Answer: never.**

## Python

```python
def __getitem__(self, i): ...
def __len__(self): ...
```

## satellite

```
satellite.capsule at(satellite.variable.number i) satellite.returns(satellite.variable.number)
satellite.capsule size() satellite.returns(satellite.variable.number)
```

## What differs

[M38](../SATELLITE/M38-operator-overloading.md) excludes `[]` and `()` on purpose ([CXX23 row 175](../CXX23/M175.md)), and there is no iterator protocol. A suit answers `size()` and `at(i)` by name.
