# PYTHON/M314 — `Condition`, `Event`, `Semaphore`, `Barrier`

**Introduced:** Python 1.5 / 3.2. Present in Python 3.12.
**Answer: satellite milestone [M40](../SATELLITE/M40-locks-and-atomics.md).**

## Python

```python
done = threading.Event()
done.wait()
```

## Why

Waiting on something needs a lock underneath. [M40](../SATELLITE/M40-locks-and-atomics.md). `join()` is the one wait that exists.
