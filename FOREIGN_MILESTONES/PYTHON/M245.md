# PYTHON/M245 — packages, `__init__.py` and relative imports

**Introduced:** Python 1.5 / 2.5. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
from .models import User
```

## Why

Follows a second file (previous row) and program namespaces. Nothing is decided.
