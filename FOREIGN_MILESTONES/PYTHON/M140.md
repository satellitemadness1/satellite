# PYTHON/M140 — `async for`, `async with`, async generators

**Introduced:** Python 3.5 / 3.6. Present in Python 3.12.
**Answer: satellite milestone [M43](../CXX26/M43.md).**

## Python

```python
async with session:
    async for item in stream: ...
```

## Why

Follows `async def`. The generator half is also row 18's *never*.
