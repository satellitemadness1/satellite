# PYTHON/M294 — `math.sin`, `cos`, `log`, `exp`

**Introduced:** Python 1.0. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
math.sin(x)
math.log(x)
```

## Why

**Not in `satl --words`.** **Open, and [CXX23 row 277](../CXX23/M277.md)**, which names the design question: an exact number has no exact `sin(1)`.
