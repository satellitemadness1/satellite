# PYTHON/M136 — `warnings.warn`

**Introduced:** Python 2.1. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
warnings.warn("config is old", DeprecationWarning)
```

## Why

The tree has a diagnostic severity that does not stop the run, and a program has no way to emit one. **Open**, alongside the stderr row [M254](M254.md).
