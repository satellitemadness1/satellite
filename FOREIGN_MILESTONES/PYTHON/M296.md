# PYTHON/M296 — `math.gcd`, `lcm`, `factorial`, `comb`, `isqrt`

**Introduced:** Python 3.5 / 3.9 / 2.6 / 3.8. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
math.gcd(a, b)
math.factorial(30)
```

## Why

Not in the word tree. A recursive `fact(30)` is exact today. **Open, and [CXX23 row 279](../CXX23/M279.md)**: small, and the arithmetic satellite numbers are best at.
