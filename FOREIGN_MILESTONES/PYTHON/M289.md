# PYTHON/M289 — `zipfile`, `gzip`, `tarfile`

**Introduced:** Python 1.6 / 1.5 / 2.3. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
with zipfile.ZipFile(p) as z:
```

## Why

Not in the word tree, and it needs binary files first. Open.
