# PYTHON/M159 — `functools.lru_cache` and `cache`

**Introduced:** Python 3.2 / 3.9. Present in Python 3.12.
**Answer: satellite milestone [M32](../SATELLITE/M32-capsules-as-values.md).**

## Python

```python
@cache
def fib(n): ...
```

## satellite

```
satellite.library.fib_seen = satellite.container.map()
```

## What differs

As a decorator it is [M32](../SATELLITE/M32-capsules-as-values.md). **The memo itself is a map today**: `has(n)`, else compute and `set`. `satellite.library` makes it outlive the call.
