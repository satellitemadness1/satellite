# PYTHON/M190 — `__slots__`

**Introduced:** Python 2.2. Present in Python 3.12.
**Answer: never.**

## Python

```python
__slots__ = ("x", "y")
```

## Why

A spacesuit's fields are already fixed by its declaration, which is what `__slots__` exists to impose.
