# PYTHON/M106 — repetition `"-" * 40` and `[0] * n`

**Introduced:** Python 1.0. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
print("-" * 40)
row = [0] * width
```

## satellite

```
satellite.variable.string rule = ""
satellite.statement.for (satellite.variable.number i = 0; i < 40; i = i + 1)
{
    rule.append("-")
}
```

## What differs

**Checked 2026-09-12: refused.** `"ab" * 3` is `S0711: * works on two numbers and
this one is string`.

**Open, together with list `+`** (next row). Both are *operators on something
that is not a number*, and [M38](../SATELLITE/M38-operator-overloading.md) limits even user operators to `+`, `==` and `<`.
