# PYTHON/M86 — `set` and `frozenset`

**Introduced:** Python 2.4. Present in Python 3.12.
**Answer: partial — satellite has part of it.**

## Python

```python
seen = set()
if name not in seen:
    seen.add(name)
```

## satellite

```
satellite.container.map<satellite.variable.string, satellite.variable.bool> seen = satellite.container.map()
satellite.statement.if (!seen.has(name)) { seen[name] = satellite.bool.true }
```

## What differs

A map with a throwaway value is a set, and `has(k)` is the membership test. Union, intersection and difference are loops (row [M240](M240.md)).
