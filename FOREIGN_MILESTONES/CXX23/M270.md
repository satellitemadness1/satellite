# CXX23/M270 — `std::iota`, `std::fill`, `std::generate`

**Introduced:** C++98 / C++11. Present in C++23 (ISO/IEC 14882:2024).
**Answer: satellite says it.**

## C++

```cpp
std::iota(v.begin(), v.end(), 0);
```

## satellite

```
satellite.statement.for (satellite.variable.number i = 0; i < n; i = i + 1)
{
    v.append(i)
}
```

## What differs

A loop of appends. `std::generate` with a function object is [M32](../SATELLITE/M32-capsules-as-values.md).
