# CXX23/M108 — `const_cast`

**Introduced:** C++98. Present in C++23 (ISO/IEC 14882:2024).
**Answer: never.**

## C++

```cpp
const_cast<T &>(x) = y;
```

## Why

There is no `const`, the same decision as [M8](M8.md), so there is nothing to cast away.
