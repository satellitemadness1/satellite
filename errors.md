satellite.return inside an if doesn't end the capsule, and inside a loop it only ends that pass.

A capsule calling itself crashes at about 5,000 deep. Following your no-limits rule, the fix is the walker keeping its own stack, not a cap.

satellite.include(./x) without quotes includes nothing, and nothing says so.

A statement outside every capsule is silently skipped.

"n = " + 4 is refused in 004. 003 joined it after your "just do it all for them" ruling, and that was never carried over.