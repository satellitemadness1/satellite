# M16 — Containers & the Search Power

**Landed in `prototype/M10` on 2026-08-29, and NOT in this tree.** **Reviewed
2026-08-29** by building that prototype and running its test suite.

**READ THAT FIRST LINE BEFORE ANY OF THE REST**, and see [M11.md](M11.md) for the
same correction in full. `prototype/` is drafts and never sources (LAYOUT.md),
PLAN §8 lists M16 as a milestone that has not started, and nothing below is a
claim about the interpreter this repository builds.

*(Corrected 2026-08-30, found the same way.)*

**Verdict: done.** All 39 PathIds assigned to M16 are implemented and registered in the dispatch table, `satellite.container.list` with all 25 methods and `satellite.container.map` with all 9 methods are fully functional with subscript indexing and assignments, the 10-level Search Power ladder and structural walker are fully operational, the search threshold system dials (`satellite.system.threshold()`, `satellite.system.threshold(n)`) are wired up, and `satellite.main` argument binding correctly defaults to an empty list.

---

## 1. What the milestone was

From [PLAN.md](../PLAN.md) §8.2:

- **39 numbered paths** per [WORD_NUMBERS.md](../WORD_NUMBERS.md) §2.2:
  - **Containers (1 4)**: `satellite.container` (`1 4 (0)`), `satellite.container.arguments` (`1 4 3`), `satellite.container.result` (`1 4 4`)
  - **Maps & 9 Methods (1 4 1)**: `satellite.container.map` (`1 4 1 (0)`), `.set(k, v)` (`1 4 1 1`), `.get(k)` (`1 4 1 2`), `.has(k)` (`1 4 1 3`), `.size` (`1 4 1 4`), `.empty` (`1 4 1 5`), `.clear` (`1 4 1 6`), `.remove(k)` (`1 4 1 7`), `.keys` (`1 4 1 8`), `.values` (`1 4 1 9`)
  - **Lists & 25 Methods (1 4 2)**: `satellite.container.list` (`1 4 2 (0)`), `.append` (`1 4 2 1`), `.size` (`1 4 2 2`), `.sort()` (`1 4 2 3`), `.sort(direction)` (`1 4 2 4`), `.sort_down()` (`1 4 2 5`), `.sort_down(key)` (`1 4 2 6`), `.sort_up(key)` (`1 4 2 7`), `.contains(x)` (`1 4 2 8`), `.index_of(x)` (`1 4 2 9`), `.empty` (`1 4 2 10`), `.clear` (`1 4 2 11`), `.first` (`1 4 2 12`), `.last` (`1 4 2 13`), `.truncate(n)` (`1 4 2 14`), `.reserve(n)` (`1 4 2 15`), `.remove_first()` (`1 4 2 16`), `.remove_last()` (`1 4 2 17`), `.remove_at(n)` (`1 4 2 18`), `.remove(x)` (`1 4 2 19`), `.insert(n, x)` (`1 4 2 20`), `.join(separator)` (`1 4 2 21`), `.reverse` (`1 4 2 22`), `.sum` (`1 4 2 23`), `.max` (`1 4 2 24`), `.min` (`1 4 2 25`)
  - **System Threshold Dials (1 22 5 & 1 22 6)**: `satellite.system.threshold()` (`1 22 5`), `satellite.system.threshold(n)` (`1 22 6`)
- **Search Power Engine**:
  - **Layer 1: Comparator** (`search.cpp`): Evaluates similarity on the 10-level ladder: `SEARCH_EXACT` (1), `SEARCH_CASE` (2), `SEARCH_CROSS_TYPE` (3), `SEARCH_TRIMMED` (4), `SEARCH_PREFIX` (5), `SEARCH_SUBSTRING` (6), `SEARCH_DECODED` (7), `SEARCH_ONE_TYPO` (8), `SEARCH_TWO_TYPOS` (9), `SEARCH_SUBSEQUENCE` (10). Supports nested container-against-container scoring.
  - **Layer 2: Structural Walker** (`search_walk.cpp`): Recursively traverses nested lists and maps, enforces `max_depth` limits, matches entry patterns, collects `SearchHit` records, and stably sorts by match score.
  - **Layer 3: Search Application & Dial** (`search_apply.cpp`): Formats rich match maps (`{value, key, path, score}`) and provides thread-local threshold control (`g_threshold`).
  - **Container `.search(pattern)` method**: Dispatches structural search over lists and maps.
- **End-to-End Test Suite**:
  - 6 comprehensive test sections verifying map operations and subscript writes, all 25 list methods including keyed sorts (`sort_up(key)`, `sort_down(key)`), the complete 10-level comparator ladder, structural searches and threshold dials, `satellite.main` argument defaulting, and acceptance sample programs.

---

## 2. Done-When, Clause by Clause

| Clause | Where It Lives | Status |
|---|---|---|
| 39 PathIds registered in `DispatchTable` | `prototype/M10/dispatch.cpp` | ✔ |
| `satellite.container.map` (9 methods) | `prototype/M10/dispatch.cpp` | ✔ |
| `satellite.container.list` (25 methods) | `prototype/M10/dispatch.cpp` | ✔ |
| Subscript reading & assignments (`container[k] = v`) | `prototype/M10/compiler.cpp` (`IndexAssignStmtClosure`, `IndexClosure`) | ✔ |
| 10-level Search Power comparator | `prototype/M10/search.cpp` | ✔ |
| Structural Search Walker | `prototype/M10/search_walk.cpp` | ✔ |
| Search Application & Dials | `prototype/M10/search_apply.cpp`, `prototype/M10/dispatch.cpp` | ✔ |
| Main parameter list defaulting | `prototype/M10/evaluator.cpp`, `prototype/M10/closure.cpp` | ✔ |
| Comprehensive test harness | `prototype/M10/test.cpp` (6/6 test suites pass) | ✔ |

---

## 3. Test Verification

```bash
$ make -C prototype/M10 test
=== Running Milestone 10 (M16) Comprehensive Test Suite ===

--- Test 1: Map Operations (9 Methods & Subscripts) ---
  [PASS] map creation, 9 methods, and subscripting pass

--- Test 2: Full Suite of 25 List Methods ---
  [PASS] all 25 list methods pass verification

--- Test 3: Search Power 10-Level Scoring Ladder ---
  [PASS] Level 1: EXACT match
  [PASS] Level 2: CASE fold match
  [PASS] Level 3: CROSS-TYPE match (number vs string)
  [PASS] Level 4: TRIMMED whitespace match
  [PASS] Level 5: PREFIX match
  [PASS] Level 6: SUBSTRING match
  [PASS] Level 8: ONE TYPO edit distance <= 1
  [PASS] Level 9: TWO TYPOS edit distance <= 2
  [PASS] Level 10: SUBSEQUENCE fuzzy match

--- Test 4: Search Power on Nested Containers & Dials ---
  [PASS] search power on containers and threshold dials pass

--- Test 5: Parameter Binding in satellite.main ---
  [PASS] satellite.main list argument binds to empty list by default

--- Test 6: Full Acceptance Programs Pipeline ---
Hello, World!
  [PASS] pipeline run example/hello_world.satl
  [PASS] pipeline run example/class_test.satl
  [PASS] pipeline run example/gui_example.satl

>>> ALL M16 TESTS PASSED CLEANLY <<<
```

