    if (key == "if")
        return
"satellite.statement.if / .else -- branching\n"
"\n"
"  satellite.statement.if (CONDITION)\n"
"  {\n"
"      STATEMENTS\n"
"  }\n"
"  satellite.statement.else\n"
"  {\n"
"      STATEMENTS\n"
"  }\n"
"\n"
"  The parentheses and the braces are both required. There is no\n"
"  one-statement form: a body without braces is \"expected '{' to open a\n"
"  block\". The else is optional.\n"
"\n"
"  THE CONDITION MUST BE A satellite.variable.bool. There is no\n"
"  truthiness anywhere in the language: 0 is not false, \"\" is not false,\n"
"  and a non-empty string is not true.\n"
"\n"
"      satellite.statement.if (1)\n"
"      condition must be a satellite.variable.bool, got\n"
"      satellite.variable.number\n"
"\n"
"  Every value has a declared type, so \"is 0 false\" is a question this\n"
"  language never has to answer.\n"
"\n"
"  THE CHECK HAPPENS WHEN THE if RUNS, not when the file is parsed. A\n"
"  wrong-typed condition inside a branch that is never taken is never\n"
"  reported and the program exits 0. A clean run is not a check of every\n"
"  condition in the file.\n"
"\n"
"  CHAINING. An else takes a block, or another if:\n"
"\n"
"      satellite.statement.else satellite.statement.if (n == 2)\n"
"      {\n"
"      }\n"
"\n"
"  There is no elif. Writing satellite.statement.elif is not even a parse\n"
"  error -- it is \"no such module function\" when that line runs.\n"
"\n"
"  EMPTY PARENS AFTER else ARE ACCEPTED, and only empty ones:\n"
"\n"
"      satellite.statement.else()      the same statement, parens dropped\n"
"      satellite.statement.else(n < 5) refused\n"
"\n"
"  19.8 accepted the first because every other statement form is written\n"
"  with parentheses, so reaching for them here is a mistake a reader makes\n"
"  once per program. A condition on an else is a misunderstanding rather\n"
"  than a spelling, so it stays refused.\n"
"\n"
"  SCOPE, AND THE ONE PLACE IT IS NOT A SCOPE. Inside a capsule or a\n"
"  spacesuit member each block is its own scope, and a variable declared\n"
"  in a branch is gone after it. AT THE TOP LEVEL IT IS NOT: top-level\n"
"  names are globals looked up at run time, so the same code outside a\n"
"  capsule leaks the name -- and leaks it DYNAMICALLY, existing only if\n"
"  the branch actually ran. Declare before the if, or use a capsule.\n"
"\n"
"  WRITING THE TWO BOOLS. satellite.bool.true and satellite.bool.false\n"
"  are canonical (8.4). Bare TRUE and FALSE also work (19.7) and are NOT\n"
"  reserved: anything of yours with that name wins the word.\n"
"\n"
"  A bool does not concatenate. \"flag=\" + b is \"+ does not apply\", while\n"
"  \"flag=\" + b.to_string() prints flag=true. (19.9 has this open.)\n"
"\n"
"  NO && AND NO ||. .and() and .or() are ordinary calls, so BOTH sides\n"
"  evaluate and neither short-circuits.\n"
"\n"
"  satellite.help(\"while\")  satellite.help(\"for\")\n";

    if (key == "else")
        return
"satellite.statement.else -- the other branch\n"
"\n"
"  satellite.statement.else\n"
"  {\n"
"      STATEMENTS\n"
"  }\n"
"\n"
"  Two spellings are accepted and mean the same thing: bare, and with an\n"
"  EMPTY pair of parentheses, satellite.statement.else(). 19.8 added the\n"
"  second after a real program wrote it 24 times by symmetry with every\n"
"  other statement form in the language.\n"
"\n"
"  A CONDITION IS STILL REFUSED. satellite.statement.else(n < 5) is\n"
"  \"expected '{' to open a block\", and that unhelpful-looking message is\n"
"  the right one to get: a condition on an else is a misunderstanding\n"
"  rather than a spelling, so the reader is sent back to the shape of the\n"
"  statement.\n"
"\n"
"  An else takes a block or a chained if, and the chained if may sit on\n"
"  the line below -- the else keeps looking past the line end even though\n"
"  statements are newline-terminated.\n"
"\n"
"  The else must follow the if's closing brace. Blank lines and //\n"
"  comments between them are fine. A STATEMENT between them detaches it,\n"
"  and a detached else is a RUN-TIME error rather than a parse error --\n"
"  \"satellite.statement.else is a module path, not a value\" -- so a\n"
"  detached else inside a branch that never runs is never reported at\n"
"  all.\n"
"\n"
"  satellite.help(\"if\") has the whole picture.\n";

    if (key == "while")
        return
"satellite.statement.while -- repeat a block while a bool holds\n"
"\n"
"  satellite.statement.while (CONDITION)\n"
"  {\n"
"      STATEMENTS\n"
"  }\n"
"\n"
"  The condition is tested BEFORE the first pass, so one that starts\n"
"  false runs the body zero times, and it is re-read at the top of every\n"
"  pass after that. There is no do-while.\n"
"\n"
"  The condition must be a satellite.variable.bool, exactly as an if's\n"
"  must be -- there is no truthiness. It is an EXPRESSION and not a\n"
"  statement, so C's while ((line = next()) != nil) does not parse: an\n"
"  assignment is a statement in this language and cannot appear here.\n"
"\n"
"  THE BODY IS ITS OWN SCOPE, and this is what the flag idiom below is\n"
"  usually got wrong on. A variable declared in the body is FRESH ON\n"
"  EVERY PASS -- a counter declared there reads 1 forever -- and it does\n"
"  not exist after the loop. The condition is read in the ENCLOSING\n"
"  scope, so a flag declared inside the body it controls is an unknown\n"
"  variable at the header. EVERY FLAG A LOOP TESTS MUST BE DECLARED\n"
"  BEFORE THE LOOP.\n"
"\n"
"  THERE IS NO break AND NO continue, under any spelling:\n"
"\n"
"      satellite.statement.break     is a module path, not a value\n"
"      satellite.statement.break()   no such module function\n"
"      break                         unknown variable in capsule\n"
"\n"
"  That is the largest gap the language still has rather than an\n"
"  oversight. 15 ranks them first among what is missing now that file I/O\n"
"  is built, and 17 holds bytecode ids 18-20 open against the day they\n"
"  exist. Today a loop is left by making its condition false:\n"
"\n"
"      satellite.variable.bool searching = TRUE\n"
"      satellite.variable.number i = 0\n"
"      satellite.statement.while (searching)\n"
"      {\n"
"          satellite.statement.if (i >= l.length())\n"
"          {\n"
"              searching = FALSE\n"
"          }\n"
"          satellite.statement.else\n"
"          {\n"
"              i = i + 1\n"
"          }\n"
"      }\n"
"\n"
"  Read that shape as what a search looks like here, not as a workaround\n"
"  somebody chose.\n"
"\n"
"  A RUNAWAY LOOP IS NOT CAUGHT. The depth guard in the evaluator bounds\n"
"  RECURSION, not iteration, so a while whose condition never goes false\n"
"  runs until the memory watchdog fires or you stop it.\n"
"\n"
"  satellite.help(\"for\")  satellite.help(\"if\")\n";

    if (key == "for")
        return
"satellite.statement.for -- the three-part, C-shaped loop\n"
"\n"
"  satellite.statement.for(satellite.variable.number i = 0; i < 10; i = i + 1)\n"
"  {\n"
"      satellite.console.display(i)\n"
"  }\n"
"\n"
"  INIT, CONDITION, STEP, separated by ';'. Init runs once; then\n"
"  condition, body, step, condition, body, step. The condition is\n"
"  evaluated fresh every trip, so a test of i < l.length() sees a list\n"
"  the body appended to. It must be a satellite.variable.bool.\n"
"\n"
"  NO NEW SYNTAX IS INTRODUCED, which is why the form was chosen: the\n"
"  init clause is the same declaration a statement line is, and the step\n"
"  clause is the same assignment. Only the ';' is new, and it is ordinary\n"
"  punctuation.\n"
"\n"
"  ALL THREE PARTS ARE OPTIONAL, and the ';' still marks an empty one:\n"
"\n"
"      satellite.statement.for(;;)           forever\n"
"      satellite.statement.for(; n < 3;)     a while, spelled for\n"
"      satellite.statement.for(;; m = m + 1) step only, no test\n"
"\n"
"  A missing condition means always true, so for(;;) is the infinite\n"
"  loop. BOTH semicolons stay required: for() and for(;) are both\n"
"  \"expected an expression, found Punct())\".\n"
"\n"
"  NO ++ AND NO +=. The step is spelled out, i = i + 1. i++ and i += 1\n"
"  are both parse errors. An assignment is a statement and never an\n"
"  expression, so it cannot be the condition either.\n"
"\n"
"  ONE CLAUSE PER SLOT. The comma is refused in both the init and the\n"
"  step; declare a second variable on the line above the loop.\n"
"\n"
"  ';' IS USED NOWHERE ELSE IN THE LANGUAGE. Statements are\n"
"  newline-terminated, so a ';' ending one is \"unexpected Punct(;) after\n"
"  the end of a statement\", and it is refused just as flatly inside an\n"
"  argument list or an index. The parser names ';' in exactly one\n"
"  function: the one parsing this header.\n"
"\n"
"  THERE IS NO for-EACH. Walk a list by index, and walk a map by\n"
"  indexing .keys() -- 8.6 makes the map insertion-ordered precisely so\n"
"  that walking one is deterministic.\n"
"\n"
"  There is no break and no continue here either. See\n"
"  satellite.help(\"while\") for what that means and what to write instead.\n";

    if (key == "network")
        return
"satellite.network -- reaching another machine\n"
"\n"
"  STATUS: DESIGNED, NOT BUILT. This topic is the design, written down so\n"
"  that the shape is settled before any of it is coded. Nothing below\n"
"  runs today. What is decided, what is blocked, and why, is all here.\n"
"\n"
"  TWO SHAPES, AND THEY ARE DIFFERENT OBJECTS\n"
"\n"
"  A CLIENT connects out, asks for one thing, reads the answer, and is\n"
"  done. A SERVER binds a port and waits for other people. Folding them\n"
"  into one constructor gives you arguments that are meaningless half the\n"
"  time, so they stay apart:\n"
"\n"
"      satellite.variable.https thing =\n"
"          satellite.network.https(\"example.com/data.json\", 443)\n"
"\n"
"      satellite.variable.https server =\n"
"          satellite.network.https(8443, \"cert.pem\", \"key.pem\")\n"
"\n"
"  THE CLIENT, which is the form that answers \"how do I receive an\n"
"  object\":\n"
"\n"
"      thing.ok()                  did it work at all\n"
"      thing.error()               and if not, why, in words\n"
"      thing.status()              200\n"
"      thing.header(\"content-type\")\n"
"      thing.length()              bytes received\n"
"      thing.body()                the bytes, as a satellite string\n"
"      thing.save(\"local.json\")    or straight to a file, never resident\n"
"      thing.close()\n"
"\n"
"  .body() can hold anything, including a PNG or a tarball, because 8.5\n"
"  gives a satellite string a raw area of 256 codes -- one per possible\n"
"  byte -- so arbitrary bytes round-trip through one uncorrupted.\n"
"  .save() exists for what you do not want in memory, which is 9's\n"
"  argument about the console one level over.\n"
"\n"
"  satellite.network.http(...) is the identical shape without TLS.\n"
"  satellite.network.new(port) is a raw TCP socket, under the same rules.\n"
"\n"
"  THE PORT IS ALWAYS WRITTEN OUT. There is no default 443 or 80. A\n"
"  hidden default is a thing you have to know, and this language says it\n"
"  at the call site instead -- the same reason 8.3.1 gives for spelling a\n"
"  file mode \"read\" rather than a bitmask.\n"
"\n"
"  A FAILURE IS A VALUE AND NOT AN ERROR. A refused connection, a bad\n"
"  certificate, a port already bound: the handle comes back holding the\n"
"  reason and you ask .ok(). 8.3.1 settled this for satellite.file and\n"
"  the argument carries unchanged -- \"does this host answer\" is a\n"
"  question a program most often wants to ASK, and failing at the call\n"
"  site makes it unaskable without killing the program that asked.\n"
"\n"
"  THE 's' MEANS THE CERTIFICATE IS VERIFIED, OR IT MEANS NOTHING\n"
"\n"
"  A client that does not verify is http with extra steps and a\n"
"  misleading name. So verification is ON, ALWAYS, WITH NO FLAG TO TURN\n"
"  IT OFF, and a bad certificate makes .ok() false and .error() say\n"
"  which check failed. That needs a CA bundle, which is a third filename\n"
"  this design carries and which most people never think to ask about.\n"
"\n"
"  This is 18's rule applied one module over. That section refuses to\n"
"  call any random tier \"secure\" and says why: \"The word secure in a\n"
"  language's documentation is load-bearing: someone will key something\n"
"  on it.\" An https object that quietly skipped verification would be\n"
"  the same lie with a shorter name.\n"
"\n"
"  WHAT IS BLOCKED, AND ON WHAT\n"
"\n"
"  Raw TCP and plain http need NO new library -- POSIX sockets are in\n"
"  libc -- so those are buildable today.\n"
"\n"
"  https is NOT. TLS needs OpenSSL or GnuTLS, and 9 treats `ldd satl`\n"
"  listing six shared objects as load-bearing: it measured 23.4 ms of\n"
"  dynamic loading per run when the interpreter linked gtk, and split the\n"
"  binary in two to stop paying it. Linking TLS would charge that same\n"
"  cost to every headless run that never opens a socket.\n"
"\n"
"  16 already designed the way out -- a native module the runtime\n"
"  dlopen()s only when a program writes satellite.include(satellite.\n"
"  network) -- and that mechanism is M7 and IS NOT BUILT. So https waits\n"
"  on infrastructure rather than on anybody's opinion, and http is the\n"
"  seam where work can start.\n"
"\n"
"  ACCEPT BLOCKS. A server that can serve one caller at a time and do\n"
"  nothing while it waits is a demonstration and not a server, so the\n"
"  server half also wants satellite.variable.thread. See\n"
"  satellite.help(\"thread\").\n";

