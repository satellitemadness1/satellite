# PYTHON/M160 — `map()`, `filter()`, `functools.reduce()`

**Introduced:** Python 1.0. Present in Python 3.12.
**Answer: satellite milestone [M36](../SATELLITE/M36-ranges.md).**

## Python

```python
lengths = list(map(len, words))
evens = list(filter(is_even, xs))
```

## Why

[M36](../SATELLITE/M36-ranges.md), which depends on [M32](../SATELLITE/M32-capsules-as-values.md). For the most common `reduce`, a sum, `xs.sum()` exists (row [M162](M162.md)).
