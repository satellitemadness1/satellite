# PYTHON/M290 — `hashlib`, `hmac`, `base64`, `uuid`, `secrets`

**Introduced:** Python 2.5 / 2.2 / 1.0 / 2.5 / 3.6. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
hashlib.sha256(data).hexdigest()
secrets.token_hex(16)
```

## Why

None are in the word tree. `satellite.random` has three generator tiers (`fast`, `normal`, `ultra`); whether any is suitable for secrets was not checked, so do not assume `ultra` is `secrets`. Open.
