# PYTHON/M298 — `statistics.mean`, `median`

**Introduced:** Python 3.4. Present in Python 3.12.
**Answer: partial — satellite has part of it.**

## Python

```python
statistics.mean(xs)
```

## satellite

```
satellite.variable.number total = xs.sum()
satellite.variable.number mean = total / xs.size()
```

## What differs

Exact, with `sum()` and `size()`. A median is `sort()` and the middle.
