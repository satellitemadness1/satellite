# PYTHON/M283 — binary mode, `seek()`, `tell()`

**Introduced:** Python 1.0. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
with open(p, "rb") as f:
    f.seek(128)
```

## Why

Files are text lines and whole reads. **Open, and [CXX23 row 296](../CXX23/M296.md)**, which pairs it with [M44](../CXX26/M44.md).
