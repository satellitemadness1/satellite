# PYTHON/M258 — `os.getcwd()`, `os.chdir()`, `os.listdir()`

**Introduced:** Python 1.0. Present in Python 3.12.
**Answer: satellite says it.**

## Python

```python
os.listdir(".")
```

## satellite

```
satellite.container.list<satellite.variable.string> here = satellite.directory.list()
```

## What differs

`satellite.directory.current()`, `change(d)`, `exists(d)`, `list()` and `list(d)`.
