# PYTHON/M206 — `str.format()`

**Introduced:** Python 2.6. Present in Python 3.12.
**Answer: never.**

## Python

```python
"{} has {} items".format(name, n)
```

## satellite

```
name + " has " + n + " items"
```

## What differs

No format strings, the same answer as f-strings (row 19).
