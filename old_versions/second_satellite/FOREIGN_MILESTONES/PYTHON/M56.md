# PYTHON/M56 — `Callable[[int], str]`

**Introduced:** Python 3.5. Present in Python 3.12.
**Answer: satellite milestone [M32](../SATELLITE/M32-capsules-as-values.md).**

## Python

```python
handler: Callable[[int], str]
```

## Why

The type of a function value. `satellite.variable.capsule` is numbered and today holds only a call waiting to run on a thread; holding a capsule to call later is [M32](../SATELLITE/M32-capsules-as-values.md).
