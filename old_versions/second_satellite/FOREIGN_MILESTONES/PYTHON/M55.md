# PYTHON/M55 — `typing.Protocol`

**Introduced:** Python 3.8. Present in Python 3.12.
**Answer: satellite milestone [M31](../SATELLITE/M31-user-generics.md).**

## Python

```python
class HasPrice(Protocol):
    def price(self) -> float: ...
```

## Why

A structural interface. `foreign.cpp` sends Java's `interface` and Rust's `trait` to [M31](../SATELLITE/M31-user-generics.md) and the decision after it, and this is the same question.
