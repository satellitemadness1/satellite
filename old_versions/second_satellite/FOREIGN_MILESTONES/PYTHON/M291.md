# PYTHON/M291 — `io.StringIO`

**Introduced:** Python 2.0 / 3.0. Present in Python 3.12.
**Answer: never.**

## Python

```python
buf = io.StringIO()
buf.write("x=")
```

## satellite

```
satellite.variable.string buf = ""
buf.append("x=")
```

## What differs

A string can be appended to in place, which is what `StringIO` is used for.
