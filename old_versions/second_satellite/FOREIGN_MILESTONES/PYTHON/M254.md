# PYTHON/M254 — `sys.stderr` and `print(..., file=sys.stderr)`

**Introduced:** Python 1.0. Present in Python 3.12.
**Answer: open — no milestone and no refusal yet; this needs a decision.**

## Python

```python
print("warning", file=sys.stderr)
```

## Why

**A program writes to one place.** Anyone using satellite in a pipeline needs this. **Open, and [CXX23 row 294](../CXX23/M294.md).**
