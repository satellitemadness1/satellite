# CXX23/M164 — `const` member functions

**Introduced:** C++98. Present in C++23 (ISO/IEC 14882:2024).
**Answer: never.**

## C++

```cpp
int size() const;
```

## Why

No `const`, the same decision as [M8](M8.md). A `call_` verb that does not change the object simply does not change it.
