# M1.5 — the window

**Landed 2026-08-27** (`9f7f71d`), and finished across the three days after it.
**Reviewed 2026-08-30**, three days late, by the renumbering pass — which is the
one fact about this note worth reading before the rest of it.

**THIS NOTE IS THE THIRD FINDING OF THE SAME KIND, AND ITS SIGN IS FLIPPED.**
[M11.md](M11.md) and [M16.md](M16.md) were labelled *landed* and were reviews of
`prototype/` drafts. This milestone was labelled **unlanded** — it sat twenty-third
in PLAN §8's build order, under the name **M11.A**, with no file in this directory
— and every clause of its done-when had been true since the day it was written. The
2026-08-30 pass that made every milestone name its commit caught the two
over-claims and missed the under-claim, because it went looking for notes with no
commit and this milestone had no note to go looking at. **A directory of reviews
cannot find a milestone that never got one**, and that is the gap the renumber
walked into rather than the one it went looking for.

*(Renamed from M11.A to M1.5 on 2026-08-30. It landed between M1 on 2026-08-26 and
M2 on 2026-08-28, so it is numbered for when it landed, which is the same shape
M4.5 already is. PLAN §8's opening carries the whole old-to-new table.)*

---

## 1. What the milestone was

From [PLAN.md](../PLAN.md) §8:

- the GTK4 + VTE binary — `satl-term`, a **fourth** binary and not a fifth
- the `.desktop` entry joins the install here (§5.3), because the entry names a
  binary and now there is one
- §4.2's table, `050-build.mk` and LAYOUT.md's build-output table all become
  **four binaries on x86-64 and two everywhere else**
- **two numbered paths**: `satellite.window.console` `1 24 2` and its
  `new(title, width, height)` `1 24 2 1` *(assigned 2026-08-28)*

**Done when** `satl-term` opens, spawns the `satl` beside it, and renders what it
prints — which today is `satl --repl` saying the prompt is not built yet.

## 2. The done-when, clause by clause

Re-run 2026-08-30 against the binaries in the tree, after the renumber changed the
strings two of these clauses print.

| clause | how it was checked | result |
|---|---|---|
| `satl-term` is built | `make` links it from `window.o` and `terminal.o` | **yes** |
| it is the fourth binary and not a fifth | `ldd satl` → **6** shared objects, `ldd satl-term` → **79** | **yes** |
| it links no runtime | its link line is `window.o terminal.o` and the GTK stack — no interpreter object | **yes** |
| it answers without a display | `env -u DISPLAY -u WAYLAND_DISPLAY ./satl-term --version` | **0** |
| what it renders today | `satl --repl` → *"the prompt is not built yet -- it lands at M22"* | **3** |
| the build tables say four and two | `050-build.mk` §11, LAYOUT.md's build-output table | **yes** |
| the `.desktop` entry installs | `060-install-tree.sh` carries the row, conditional on the binary | **yes** |

**The one clause not re-checked here is the window actually opening**, because
that needs a display and this review was run from a terminal. It was demonstrated
by the author on 2026-08-27 and the binary has been in use since; what is recorded
above is everything that can be checked without one, which is the honest boundary
and not a claim that the rest was skipped.

## 3. What building it found

### 3.1 A fourth binary, and the argument that it is not a fifth

`satl-term` links the window and **nothing of the runtime**, and spawns the
installed `satl` into its PTY. It never interprets, so it has nothing for `-march`
to act on: it is built once at the baseline like `satl-cpu-level`, and it gets the
haswell interpreter for free by spawning whichever `satl` the installer chose.
**That is why the microarchitecture split does not double it**, and it is the whole
of §4.4 in one line — `satl` resolves 6 shared objects and `satl-term` 79.

### 3.2 It is the same window the language hands out

This is why it is a milestone and not a build artefact. A program asks for one with

    satellite.variable.window my_console =
        satellite.window.console.new("window_title", 800, 600)

— a string and two numbers — so the title and the size are **arguments** in
`satl-term` too, reached as `--title` and `--size 800x600`. The binary is the first
caller of that signature and must not be a special case of it; **M24's `dlopen`'d
library calls the same three values in.** `satellite.window.console` `1 24 2` is a
*different node* from `satellite.window.new` `1 24 1`, which is still M24's and
still reached by nothing that names it.

### 3.3 The handover, and the obvious test that is wrong

*(Built 2026-08-28, `c57ac3f`, one day after the binary.)* `satl` started with no
console has nowhere to print, and a program that runs correctly and shows nothing
is PLAN §1.1's failure exactly — so it hands itself to `satl-term`.

**The test is a controlling terminal and not `isatty(stdout)`.** The obvious
version breaks every pipeline: `satl --words | grep console` has a pipe on stdout,
answers *no*, and would open a window instead of feeding the pipe. The question
that distinguishes a launcher from a terminal is whether the process has a
**controlling** terminal, and `/dev/tty` is the file that answers it.

**There is no loop and it is not luck.** `satl-term` spawns its child on a pty, so
the child *has* a controlling terminal and answers the other way on the first line
it asks. The recursion terminates on a fact about the process, not on a flag.

`src/programs/window_handover.cpp` names all six refusals, numbered in the source:
**ONE** the user said not to (`--no-window`, `SATL_NO_WINDOW=1`); **TWO** there is
a console; **THREE** no console but the output is going somewhere on purpose;
**FOUR** no display; **FIVE** no `satl-term` beside us, which is real because
`047-window.mk` makes it conditional; **SIX** `execv` failed, so carry on here and
print nothing about it.

### 3.4 The window closes on a clean exit and **holds** on a failure

Until there is a prompt the child runs for milliseconds, and a window that outlived
every one of them would only ever be a window nobody asked to keep. So it closes on
a clean exit — and holds on a failure, because **a failed child is holding the only
copy of the reason and destroying the window destroys the message.**

That is what makes this milestone demonstrable before the prompt exists: `satl
--repl` answers *"not built yet"* and exits `EXIT_NOT_YET`, so the window stays up
with the explanation on it.

**M22 removes the clean-exit arm rather than discovering it.** `on_child_exited` in
`src/programs/terminal.cpp` is the one function that changes, and it is written
knowing this — the clean-exit arm is marked as this milestone's.

## 4. What is left open

- **The prompt.** `satl-term` renders a message saying the prompt is not built.
  That is M22, and it is where the window stops closing.
- **`satellite.window.new` `1 24 1`** is not this milestone's and is not built. It
  is M24's, behind `libsatellite_window.so`, and nothing that names it exists yet.
- **The two paths are registered and not answerable.** M2 put
  `satellite.window.console` `1 24 2` and `new(...)` `1 24 2 1` in `words.def` on
  2026-08-28; the binary calls the signature, the *language* cannot yet. PLAN §8.2
  counts them here because this is the milestone that makes them mean something.
- **`terminal.cpp` is 236 lines** and is the largest C++ file this milestone left
  behind. Inside PLAN §3's 300-line target, and the number to watch.

## 5. What this note closes elsewhere

- **PLAN §8** moves this entry out of the twenties and into the landed block, and
  §1's "where things stand" now lists 2026-08-27 between M1 and M2.
- **[README.md](README.md)'s table** gains a row with a date in it rather than
  **not in this tree**, which is the column that exists to tell those apart.
- **The convention this directory states — a milestone with no commit is either
  unlanded or mis-labelled — gains its third case and its first in this direction.**
  The rule as written catches a note that claims too much. It does not catch a
  milestone with no note at all, and nothing in the tree did until the numbers were
  put back into build order and this one was found sitting seventeen positions past
  the work in progress.
