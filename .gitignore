# satellite 004 -- build output and scratch. Nothing here is source.
/build/
__pycache__/
*.o
*.so

# The author's timing test builds beside its source.
/polymorph/test
/polymorph/out.txt

# old_versions/second_satellite/ keeps its own .gitignore for 003's build output.

# The first satellite and the prototype, kept on disk to read and DELIBERATELY NOT
# IN THIS REPOSITORY'S HISTORY -- the second satellite's .gitignore said why: whole
# trees with their own history elsewhere, 85 MB and 17 MB. old_versions/second_satellite/
# IS tracked: it was this repository until 2026-09-15.
/old_versions/first_satellite/
/old_versions/prototype/
