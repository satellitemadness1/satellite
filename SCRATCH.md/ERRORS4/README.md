# Errors found in satellite 004 (satl build 0099)

One page per error. Found on 2026-09-26 by running thousands of ordinary satellite programs
against satl (revision 08, build 0099) and re-checking each suspected error: a second agent
re-ran it, shrank it to the smallest program that still shows it, and checked it against
`ERROR.md`, `errors.md`, `SCRATCH.md/ERRORS2.md`, `NEW_ERROR_LIST.md`, `CONTAINERS.md`,
`DESIGN.md` and the help topic. What was already known or is by design is left out.

Verdicts on the 178 suspected errors: 127 confirmed-new, 42 already-known, 5 not-an-error, 3 by-design, 1 not-reproduced.
After merging the ones that are one error: **85 pages**.

The runner, the raw verdicts and this script are in `hunt-data/` beside this page. The hunters'
own programs (2,258 of them, 460 MB with their bytecode) stayed on the author's machine at
`/home/madness/Documents/satl/hunt-data/probes/`; each page carries its smallest program.

**NOT YET RE-CHECKED.** Every page was true of build 0099. What landed after it (0da03d9,
5b8cdf3, 15f5119) and the two branches not yet merged (`m16-string-methods`,
`errors-2026-09-26`) may fix some; run each page's program on today's satl before fixing it.

| # | error | severity | kind | area |
|---|---|---|---|---|
| 001 | [Without .lock(), two threads writing one shared object field (string, list, big number) or appending to one file take no hold at all: glibc heap corruption (134), segfault (139) or a false S999, and the appended file is left empty](001-unlocked-shared-writes-corrupt-heap.md) | high | crash | threads |
| 002 | [A bare block ({} or { ... }) ends the enclosing body at its }: the rest of main (satellite.return included) is skipped with exit 0, and inside a while the loop never advances (hang)](002-bare-block-ends-enclosing-body.md) | high | wrong-answer | syntax and layout |
| 003 | [A statement that starts with a code that cannot start one (a sign, a literal, !, /*, )) is stepped over one code at a time and accepted: a continuation line starting with - or + is silently dropped (100 / - 30 holds 100), -n = 9 runs as n = 9, and a /* ... */ block runs its contents](003-unrecognised-statement-start-skipped.md) | high | wrong-answer | integers, syntax and layout |
| 004 | [A capsule or library value named with one spelling of a method word is folded to that word's first spelling: two capsules color/colour in one spacesuit reach only one of them, m.as_number() runs number(), library values contain/contains collide, and library.upper reads a value written as up](004-method-word-capsule-spellings-folded.md) | high | wrong-answer | spacesuits and library |
| 005 | [A spacesuit capsule named lock() or unlock() never runs: the built-in object lock silently takes its place, and the declaration is not refused](005-lock-unlock-capsule-shadowed.md) | high | wrong-answer | spacesuits and library |
| 006 | [Lines appended through one file handle stay in its 64 KiB buffer when a second handle or satellite.info reads the path: the reader sees too few lines, and the second handle's writes land before the held lines, out of program order](006-file-append-buffer-not-flushed.md) | medium | wrong-answer | files and io |
| 007 | [A method on a negative literal runs before the touching minus: -5.add(3) is -(5.add(3)) = -8, not -2, and -5.string is refused as "a minus sign was put in front of a string"](007-negative-literal-method-precedence.md) | medium | wrong-answer | number string conversion |
| 008 | [.upper() and .reverse() on a coloured string rewrite its embedded terminal codes, so raw escape bytes reach a pipe (ESC[38;2;0;255;0M), and at a terminal ESC[...M deletes lines](008-case-and-reverse-break-colour-codes.md) | medium | wrong-answer | strings |
| 009 | [satellite.info.file, satellite.info.directory and satellite.directory.list resolve a relative path against the working directory, while satellite.file.* resolve it against the program's folder, so info can describe a different file from the one that was opened](009-info-paths-resolve-against-working-directory.md) | medium | wrong-answer | files and io |
| 010 | [List item matching (.contains, .index_of, .remove, list == list, index == index) never matches values of different kinds that == calls equal: a[3] == 3.0 is true, but a.contains(3.0) is false](010-list-matching-ignores-equality.md) | medium | wrong-answer | lists |
| 011 | [satellite.console.input() takes its line as raw bytes: it keeps the \r of a CRLF line ("12\r" refuses .number and != "12") and a leading UTF-8 BOM, both of which satellite.file drops](011-console-input-not-normalised.md) | low | wrong-answer | files and io, number string conversion |
| 012 | [map.sort().by_value(), .by_name() and .reverse() silently work on the keys and answer a list (ranking a word count by value gives an alphabetical list), while .max/.min/.sum/.join on a map ask which half is meant](012-map-sort-and-reverse-use-keys.md) | low | wrong-answer | maps and nesting, realistic programs |
| 013 | [satellite.access drops the inner 'when' condition for a multiple nested under another multiple, so x[n][m] is shown as working when x[n] is a number](013-access-nested-multiple-drops-condition.md) | low | wrong-answer | maps and nesting |
| 014 | [satellite.access shows a string (a key, or a plain string name's value) without escaping " or \, and shows a tab or newline as \x09 / \x0a, a form satl refuses, so different strings look identical](014-access-strings-unescaped.md) | low | wrong-answer | maps and nesting, strings |
| 015 | [A container holding a see-through colour shows its ", 50" with no delimiter, so the list reads as if it had an extra item](015-colour-transparency-in-container-display.md) | low | wrong-answer | binary hex color infinity bool |
| 016 | [A // comment in a place where only line ends are stepped over breaks an if/else/while/for: a comment line between a header and its {, a comment after else, or a comment after an if's } or before the else gives "has no body" or "else with no satellite.statement.if before it"](016-comment-not-stepped-over-in-if-else.md) | medium | valid-program-refused | control flow and capsules, realistic programs, syntax and layout |
| 017 | [Conversions made when a whole name is assigned (binary and hex to number, whole number to float) are skipped for a capsule parameter, a container item and a for counter: a number parameter or list slot keeps b0011, a list slot refuses a hex, list<float> refuses 3, and a for counter refuses k = b0001](017-store-conversions-skipped-outside-assignment.md) | medium | valid-program-refused | binary hex color infinity bool, control flow and capsules, lists |
| 018 | [A method on a bare value word (satellite.bool.true, satellite.console.width, satellite.library.main.arguments.<fact>) is refused at run time with S110 and a false "math sign spacing" hint, while the same value in brackets or in a variable takes the method](018-method-on-bare-value-word-refused.md) | medium | valid-program-refused | number string conversion |
| 019 | [A blank or comment line between satellite.return(satellite) and main's } is refused as S103 "the file could not be re-read", which asks for the return that is already there](019-blank-line-before-main-brace-s103.md) | medium | valid-program-refused | syntax and layout |
| 020 | [Include paths are compared without normalising them: one file included as shapes and as ./shapes is called "two files ... rename one", and a mutual include through ./ grows the path until "cannot locate file"](020-include-paths-not-normalised.md) | medium | valid-program-refused | syntax and layout |
| 021 | [satellite.thread.new(list[i].method()) and thread.new(map[key].method()) are refused with a false S721 saying the container "names a capsule and does not call it"](021-thread-new-indexed-item-refused.md) | medium | valid-program-refused | threads |
| 022 | [A touching operator (n+1, 4+3, n**3, a*2) is refused only when its line runs: it is accepted silently in a branch not taken or a capsule never called, and refused after earlier lines have printed](022-touching-sign-refused-only-at-run.md) | medium | invalid-accepted-silently | integers, syntax and layout |
| 023 | [s.reverse("x"), "abc".reverse(1, 2, 3) and n.reverse(99) are accepted silently with the argument ignored, while s.upper(1) is refused before anything runs](023-reverse-arguments-ignored.md) | medium | invalid-accepted-silently | strings |
| 024 | [On a colour's declaring line, a colour variable whose name is spelled in hex letters and followed by a method (bead.transparency(20)) is refused as "read here as hex digits"](024-colour-line-hex-named-variable-method.md) | low | valid-program-refused | binary hex color infinity bool |
| 025 | [A container type split after its < (list< newline number>) is not joined across lines: "a type was expected here"](025-type-angle-bracket-not-joined.md) | low | valid-program-refused | syntax and layout |
| 026 | [A file whose name looks like a hex or binary literal (x1, xa, b1) cannot be included: include(x1) "names no file", include("x1") then gives "no capsule named area", and include(./x1) looks for ./.satl](026-hex-named-file-cannot-be-included.md) | low | valid-program-refused | syntax and layout |
| 027 | [An else after a } that does not close an if (a while's or for's body, or a second else) passes the checker and is refused only when reached, as a one-line message with no S-code or line](027-else-after-non-if-brace-accepted.md) | low | invalid-accepted-silently | control flow and capsules, syntax and layout |
| 028 | [A map declared with a key type that can never be a key (list, map, float) is accepted, and satellite.access prints a fill line (m[key] = 1) that is always refused](028-map-key-type-unchecked.md) | low | invalid-accepted-silently | maps and nesting |
| 029 | [List .max, .min and .sort().by_value() put bools in order and succeed, while < and > refuse two bools because "only == and != order those"](029-bools-ordered-by-max-min-sort.md) | low | invalid-accepted-silently | lists |
| 030 | [A statement that only computes a method's answer (a.sort().by_value(), a.reverse(), a.sum, n.add(1)) is accepted and does nothing, while a bare a or a + 1 is refused as doing nothing](030-discarded-method-answer-accepted.md) | low | invalid-accepted-silently | lists |
| 031 | [Building a string with s = s + ... in a loop is quadratic: every + copies the whole left side (40,000 passes take 9.8 s for 230 KB)](031-string-append-quadratic.md) | medium | performance | number string conversion |
| 032 | [Each map removal is O(n) (take_entry_out renumbers every row of the hash, and .remove(key) scans the entries), so emptying a map is quadratic: 20,000 remove_last() take 3.5-4 s, against 0.2 s for a list](032-map-remove-linear.md) | medium | performance | maps and nesting |
| 033 | [Passing a list<number> to a list<number> parameter or declaration re-checks every item each time: 20,000 calls with a 20,000-item list take 15+ s typed, against 0.19 s untyped](033-typed-list-rechecked-on-every-bind.md) | medium | performance | realistic programs |
| 034 | [Every started thread stays in the process-wide registry until satellite.return: a joined thread keeps its answer (614 MB at 80 rounds, against 44 MB for direct calls), and a finished thread that was never joined keeps ~17 KB of stack (1 GB at 60,000)](034-thread-registry-keeps-every-thread.md) | medium | performance | threads |
| 035 | [A comment line after a capsule's last self-call stops it being a tail call: memory grows with depth (1.1 GB at 300,000 levels, 10.9 GB at 3,000,000; 18 MB without the comment)](035-comment-after-self-call-breaks-tail-call.md) | medium | performance | control flow and capsules |
| 036 | [Numbered but unbuilt method words that have no method code (.abs .sqrt .floor .digits, .to_binary on a hex, .at .substring .split .trim .starts_with .ends_with .resolved) are refused as "not one of its methods -- only a spacesuit has capsules", "no capsule named X" or S201, not S210 NOT_BUILT_YET](036-unlisted-method-words-not-s210.md) | medium | contradicts-help | binary hex color infinity bool, integers, realistic programs, strings |
| 037 | [A name spelled like a hex or binary literal (x1, xa, xff, b0, b1, b10) cannot be declared or used as a parameter, and the refusal either quotes a naming rule the name meets ("not a name -- a name is made of a-z, A-Z, 0-9 and _") or says there is no name](037-hex-shaped-name-false-name-rule.md) | medium | misleading-refusal | control flow and capsules, integers, realistic programs, syntax and layout |
| 038 | [A capsule whose name is not a name (x1, xa, b1, 5) gets no refusal on its header: the capsule is dropped silently, and the first line of its body is refused as "outside every capsule" or "satellite.return(satellite) goes inside satellite.main"](038-bad-capsule-name-not-refused.md) | medium | misleading-refusal | control flow and capsules, syntax and layout |
| 039 | [An expression that stops early for any reason gets the fixed "& \| << >> !! are undecided" (or "math sign spacing") sentence at run time: n = n -1, n = 5 6, if (n = 5), 5., 0x1F, 0b101, and 1.2.3 with its caret past the end of the line](039-not-read-to-the-end-blames-undecided-operators.md) | medium | misleading-refusal | binary hex color infinity bool, floats fractions percent, integers |
| 040 | [satellite.directory.list() in a program is refused at run time with "satellite has no list type built yet to hold them (MILESTONES M14)", though M14's list is built](040-directory-list-false-m14-refusal.md) | medium | misleading-refusal | files and io |
| 041 | [satellite.console.display() with no value gets past the check and stops at run time as S210 NOT_BUILT_YET, while two values are refused before anything runs](041-empty-display-passes-check.md) | medium | inconsistency | files and io |
| 042 | [A capsule called on an empty spacesuit field is refused with a false math-sign / undecided-operator reason, and the "holds no object yet" refusal is unreachable](042-empty-field-capsule-false-reason.md) | medium | misleading-refusal | spacesuits and library |
| 043 | [Displaying or joining a list of objects whose spacesuit declares to_string() is refused because "an object has no to_string capsule"](043-object-to-string-capsule-ignored.md) | medium | misleading-refusal | spacesuits and library |
| 044 | [The checker does not judge a member reached through a map item or a nested container item (m[k].x, m[k].secret(), a wrong argument count): a field read is S201 "undeclared variable", and a protected capsule or a wrong count is refused only after earlier lines ran. Through a list item, both are refused before anything runs](044-map-item-members-unchecked.md) | medium | contradicts-help | spacesuits and library |
| 045 | [[n] after a call's answer, a method's answer, a list literal or a bracketed expression is refused at run time with a false math-sign / "& \| << >> !!" hint, though the same value in a variable can be indexed](045-index-after-answer-refused.md) | medium | misleading-refusal | lists |
| 046 | [A not-built method on a list item or on a method's answer (names[1].size, a.join(", ").size) is refused only at run time, and the refusal names the list (names.size), which is a built word](046-item-method-refusal-late-and-misnamed.md) | medium | misleading-refusal | lists |
| 047 | [Reading one character, s[1], passes the checker and is refused only at run time as S301 TYPES_DO_NOT_MEET, though the string help says it is refused with S210 before anything runs](047-string-index-s301-at-run.md) | medium | contradicts-help | realistic programs, strings |
| 048 | [find/add/to_string/to_number/to_binary/to_hexadecimal on a declared name pass the checker whatever the receiver type and argument count (p.find on a percentage, a.add on a list, s.add() on a string), and are refused only after earlier lines ran](048-string-number-methods-skip-receiver-check.md) | low | inconsistency | floats fractions percent, lists, strings |
| 049 | [The S210 hint from so_far_whose knows only file and container methods: .width on a binary and .transparency on a non-colour say "so far no type has it" (hex and colour do have them), and x.lock() says "so far it is a file's" (objects have it too)](049-so-far-whose-hint-incomplete.md) | low | misleading-refusal | binary hex color infinity bool, threads |
| 050 | [A misspelled satellite word gets "did you mean" only at the start of a statement: as a type inside <...> or as a parameter type it is "<prefix> is not a type", and in value position (satellite.bool.flase) it is an undeclared variable](050-did-you-mean-only-at-statement-start.md) | low | misleading-refusal | binary hex color infinity bool, lists |
| 051 | [A percentage, binary or hex name given an expression is told "ERROR: expected <first number><suffix>", built from its first literal alone: p = 1/4 is "expected 1%", p = 2 * 25% is "expected 2%", and a binary given a sum is "expected b1"](051-expected-suffix-quotes-first-literal.md) | low | misleading-refusal | binary hex color infinity bool, floats fractions percent |
| 052 | [Digits touching letters (1_000, 1e5, 1e-10, 5abc, 00ff00) are refused as an undeclared name made of the trailing fragment (_000, e, ff00). When such a name is declared, they pass the checker and fail at run time](052-number-touching-letters-undeclared-fragment.md) | low | misleading-refusal | binary hex color infinity bool, control flow and capsules, floats fractions percent, integers |
| 053 | [The ! operator works as bool negation, but the only help page that mentions it lists it as COMING and says "None of this reads today"](053-bang-help-says-coming.md) | low | contradicts-help | binary hex color infinity bool, control flow and capsules |
| 054 | [.bin/.hex on a declared float name pass the checker and are refused with S210 only after earlier lines ran, while f.add and a fraction's .bin/.hex are refused before anything runs](054-float-bin-hex-refused-late.md) | low | inconsistency | number string conversion |
| 055 | [.reverse() on a float or a fraction is refused as "not built yet" but filed as S301 TYPES_DO_NOT_MEET (exit 27) rather than S210 (exit 14), and only when its line runs](055-reverse-not-built-filed-s301.md) | low | misleading-refusal | number string conversion |
| 056 | [A string + an infinity is refused with the generic "no scenario for that pair" and no .string hint, although infinity.string() joins and a colour gets the hint](056-string-plus-infinity-no-hint.md) | low | inconsistency | number string conversion |
| 057 | [When a number comes first and the text spells a number, a refused pair is described by the converted value: 50% + "5" is quoted as "50% + 5", and x1F + "2.5" is called "a hex and a float"](057-refused-pair-quotes-converted-text.md) | low | misleading-refusal | number string conversion |
| 058 | [A list or map literal with a comma missing between two items is refused as "opened with { and never closed with }", though the line closes it](058-missing-comma-called-unclosed-list.md) | low | misleading-refusal | lists |
| 059 | [a[1] += 2 (a compound assignment to an indexed item) gets an unrelated S110 "not a method call" sentence instead of S210 "not built yet -- write a[1] = a[1] + ..."](059-item-compound-assign-s110.md) | low | misleading-refusal | lists |
| 060 | [The typed constructor satellite.container.list<type>() (and index<k, v>()) is refused only at run time, after earlier lines ran, as "there is no value here to work with"](060-typed-constructor-refused-at-run.md) | low | misleading-refusal | lists |
| 061 | [A typed-container refusal names the declared type without its <type> ("was declared satellite.container.list"), though a bare list holds anything](061-declared-type-shown-without-arguments.md) | low | misleading-refusal | lists |
| 062 | [The list help defines .sum as "every item added with +", but .sum refuses any string item, although 1 + "2" is 3 and "a" + "b" is "ab"](062-sum-refuses-strings.md) | low | contradicts-help | lists |
| 063 | [a.truncate(-1) is refused with "items count from 1", though truncate takes a count and truncate(0) is valid](063-truncate-negative-position-wording.md) | low | misleading-refusal | lists |
| 064 | [A file word's text argument converts a whole number (f.append(42)) but refuses a float, percentage, hex, binary or fraction as "takes text", while "" + 2.5 joins](064-file-text-argument-refuses-float.md) | low | inconsistency | files and io |
| 065 | [f.path answers the absolute resolved path, not the path as written, contrary to SATELLITE_FILE_OPERATIONS.md](065-file-path-absolute.md) | low | contradicts-help | files and io |
| 066 | [display of a file is S210 NOT_BUILT_YET with no reason, while a list holding the file gets S301 with "a file has two strings ... write the one you mean"](066-display-file-s210-no-reason.md) | low | inconsistency | files and io |
| 067 | [A .number refusal on an argument row names "that argument.number" instead of what was written (args.argument_1.number)](067-argument-row-method-placeholder-name.md) | low | misleading-refusal | files and io |
| 068 | [A trailing operator before ) (display(n +)) is told "this + has none" [space], and a touching-sign refusal inside display puts its caret at column 0](068-trailing-operator-before-bracket.md) | low | misleading-refusal | integers |
| 069 | [A refusal on ** names ^, a sign the line does not contain (0 ** -1 is "the ^ of 0 and -1")](069-power-refusal-names-caret.md) | low | misleading-refusal | integers |
| 070 | [A fraction name given a spaced slash is always told it is "whole-number division", even for 1.0 / 3, and after 2/4 it names a pair "4 / 2" that was never written](070-fraction-slash-sentence-wrong.md) | low | misleading-refusal | floats fractions percent |
| 071 | [display of a spacesuit object is refused as S210 only when its line runs, after earlier lines have printed](071-display-object-refused-late.md) | low | inconsistency | spacesuits and library |
| 072 | [counter.bump() on a spacesuit's own name says counter has no satellite.variable line declaring it](072-spacesuit-name-method-undeclared.md) | low | misleading-refusal | spacesuits and library |
| 073 | [Writing a field through a container item (things[1].v = 5, m["a"].v = 5) gets an unrelated S110 "not a method call" sentence, while reading it, or writing a.v, gets S230](073-item-field-write-s110.md) | low | misleading-refusal | spacesuits and library |
| 074 | [other.field inside the spacesuit's own capsule is refused because "fields are reached from inside the spacesuit only", though the line is already inside it](074-own-capsule-other-field-sentence.md) | low | misleading-refusal | spacesuits and library |
| 075 | [A map's .insert/.remove_at/.truncate refusal suggests age.keys.insert(...), a line that is itself refused (S110 "has no name to change")](075-map-position-method-bad-suggestion.md) | low | misleading-refusal | maps and nesting |
| 076 | [Refusals about a name declared satellite.container.map call it an "index" ("the index is empty"), though the help says a refusal uses the word that was written](076-map-called-index-in-refusals.md) | low | contradicts-help | maps and nesting |
| 077 | [On a declared map, .append/.insert/.remove_at/.truncate/.index_of/.search pass the checker and are refused only after earlier lines ran, while .sum/.max/.min/.join/.reserve are refused before anything runs](077-map-position-methods-refused-late.md) | low | inconsistency | maps and nesting |
| 078 | [A multiple refusing a container whose items are the wrong kind says only "it holds a map/list, and this name takes ...", dropping the real reason](078-multiple-drops-inner-reason.md) | low | misleading-refusal | maps and nesting |
| 079 | [satellite.access position letters collide with the name shown: a list named m is shown as m[n][m], and a list named n as n[n]](079-access-position-letters-collide.md) | low | misleading-refusal | maps and nesting |
| 080 | [satellite.return(satellite)} as main's last line is refused S103 at a line past the end of the file, which asks for the return that is already there](080-return-with-brace-s103.md) | low | misleading-refusal | syntax and layout |
| 081 | [The run-time S240 (an answer was used, but none was handed back) names the capsule's header line, not the call whose answer was used](081-s240-names-header-line.md) | low | misleading-refusal | control flow and capsules |
| 082 | ["abc".replace("a", "b") on a string literal is refused (S210) only after earlier lines ran, while s.replace on a name, and .size/.contains on a literal, are refused before anything runs](082-literal-replace-refused-late.md) | low | inconsistency | strings |
| 083 | [display(t.join()) of a thread that handed nothing back is refused at run time as S210 NOT_BUILT_YET, while display(greet()) is S240 before anything runs](083-display-thread-nothing-s210.md) | low | misleading-refusal | threads |
| 084 | [A thread joining itself is refused as S110 LINE_NOT_UNDERSTOOD, not S728 WAIT_NEVER_ENDS](084-self-join-wrong-code.md) | low | misleading-refusal | threads |
| 085 | [S728 WAIT_NEVER_ENDS for a circle made only of joins explains it as "the object's lock it needs is held by a thread", though no lock is involved](085-s728-paragraph-assumes-lock.md) | low | misleading-refusal | threads |
