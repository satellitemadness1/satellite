{
 "1 22": {
  "query": "satellite.help(satellite.system)",
  "prose": "The machine the program is running on, and the interpreter's own switches.\n\nMost of what is here describes memory, and the rest is what the machine and\nthe session are: where home is, what the environment holds, how loose a\nsearch may be, and whether the prompt keeps what a line declares.",
  "example": "satellite.console.display(satellite.system.memory.total(\"gb\"))"
 },
 "1 22 0": {
  "query": "satellite.help(satellite.system)",
  "prose": "The bare shape. There is nothing to call; one of the words underneath is\nwhat you write.",
  "example": ""
 },
 "1 22 1": {
  "query": "satellite.help(satellite.system.delete)",
  "prose": "Removes a file, or an **empty** directory, and answers whether it is gone.\n\nIt takes either a path or an open file, so a program already holding a file\ndoes not have to say its name a second time.\n\n**It is under `satellite.system` because it is one word about two kinds of\nthing.** A `satellite.file.delete` and a `satellite.directory.delete` would\neach be half an answer, and a program holding a path very often does not know\nwhich of the two it has.\n\n**It does not go into a directory, and there is no way to ask it to.** A\nremoval that quietly descended is the one mistake here nobody could take back.\nWalk `satellite.directory.list` and say each name out loud instead.\n\nFalse always means \"it is still there\" -- nothing was at that path, or the\ndirectory was not empty, or it was not yours to remove.",
  "example": "satellite.variable.file f = satellite.file.new(\"delete_demo.txt\")\nf.close()\nsatellite.console.display(satellite.system.delete(\"delete_demo.txt\"))\nsatellite.console.display(satellite.system.delete(\"delete_demo.txt\"))"
 },
 "1 22 2": {
  "query": "satellite.help(satellite.system.environment)",
  "prose": "The environment the program was started in, as a map of every variable.\n\nFor a single variable, write `satellite.system.environment(name)`, which\nanswers nothing when it is not set.",
  "example": "satellite.container.map<satellite.variable.string, satellite.variable.string> all = satellite.system.environment\nsatellite.console.display(all.size() > 0)"
 },
 "1 22 3": {
  "query": "satellite.help(satellite.system.home)",
  "prose": "The home directory of whoever is running the program.\n\nAsked of the environment first and of the password database when that is\nempty, because `$HOME` can be unset or forged and the password database is\nthe authority.",
  "example": "satellite.console.display(satellite.system.home)"
 },
 "1 22 4": {
  "query": "satellite.help(satellite.system.memory)",
  "prose": "How much memory there is, in every sense of the question: the machine's,\nthis program's, the swap file's, and this thread's stack.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.total(\"gb\"))"
 },
 "1 22 4 0": {
  "query": "satellite.help(satellite.system.memory)",
  "prose": "The bare shape. There is nothing to call; one of the words underneath is\nwhat you write.",
  "example": ""
 },
 "1 22 4 1": {
  "query": "satellite.help(satellite.system.memory.bit)",
  "prose": "How wide the memory bus is, in bits — 64 on an ordinary machine, 72 where\nthe memory carries error correction.\n\n**It answers 0 unless satellite is running as root**, and 0 is the truthful\nanswer rather than a failure: this comes from the firmware's SMBIOS table,\nwhich is readable by root alone on every ordinary Linux.",
  "example": "satellite.console.display(satellite.system.memory.bit)"
 },
 "1 22 4 2": {
  "query": "satellite.help(satellite.system.memory.frequency)",
  "prose": "How fast the memory is clocked, in MT/s.\n\n**It answers 0 unless satellite is running as root**, for the same reason\n`bit` does: the firmware table it comes from is root-only.",
  "example": "satellite.console.display(satellite.system.memory.frequency)"
 },
 "1 22 4 3": {
  "query": "satellite.help(satellite.system.memory.main)",
  "prose": "This program's own memory — its resident set, read fresh every time you\nask, so a loop can watch itself grow.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.main(\"kb\") > 0)"
 },
 "1 22 4 9": {
  "query": "satellite.help(satellite.system.memory.main)",
  "prose": "This program's own memory, in the unit you name.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.main(\"kb\") > 0)"
 },
 "1 22 4 4": {
  "query": "satellite.help(satellite.system.memory.swap)",
  "prose": "The swap file, which is the disk the machine uses when memory runs out.\n\n**A machine with no swap answers 0 to all three**, and that is truthful\nrather than a failed read.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": ""
 },
 "1 22 4 4 0": {
  "query": "satellite.help(satellite.system.memory.swap)",
  "prose": "The bare shape. One of the words underneath is what you write.",
  "example": ""
 },
 "1 22 4 4 1": {
  "query": "satellite.help(satellite.system.memory.swap.free)",
  "prose": "How much swap is unused.",
  "example": "satellite.console.display(satellite.system.memory.swap.free(\"mb\") >= 0)"
 },
 "1 22 4 4 2": {
  "query": "satellite.help(satellite.system.memory.swap.total)",
  "prose": "How much swap there is altogether.",
  "example": "satellite.console.display(satellite.system.memory.swap.total(\"mb\") >= 0)"
 },
 "1 22 4 4 3": {
  "query": "satellite.help(satellite.system.memory.swap.used)",
  "prose": "How much swap is in use.\n\n**Total minus free, and not total minus available** as the machine's own\nmemory is: swap holds no reclaimable cache, so there is nothing for\n\"available\" to mean here that \"free\" does not already mean.",
  "example": "satellite.console.display(satellite.system.memory.swap.used(\"mb\") >= 0)"
 },
 "1 22 4 4 4": {
  "query": "satellite.help(satellite.system.memory.swap.free)",
  "prose": "How much swap is unused, in the unit you name.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.swap.free(\"gb\") >= 0)"
 },
 "1 22 4 4 5": {
  "query": "satellite.help(satellite.system.memory.swap.total)",
  "prose": "How much swap there is altogether, in the unit you name.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.swap.total(\"gb\") >= 0)"
 },
 "1 22 4 4 6": {
  "query": "satellite.help(satellite.system.memory.swap)",
  "prose": "The swap file, reported in the unit you name — which is how much\nof it there is altogether.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.swap(\"gb\") >= 0)"
 },
 "1 22 4 5": {
  "query": "satellite.help(satellite.system.memory.this)",
  "prose": "This thread's own stack, as against the machine's memory.\n\n**The running thread's, and deliberately so.** Linux accounts an address\nspace per process, so a heap-per-thread figure would be the same number for\nevery thread and would answer nothing.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": ""
 },
 "1 22 4 5 0": {
  "query": "satellite.help(satellite.system.memory.this)",
  "prose": "The bare shape. One of the words underneath is what you write.",
  "example": ""
 },
 "1 22 4 5 1": {
  "query": "satellite.help(satellite.system.memory.this.available)",
  "prose": "How much room this thread still has on its stack.",
  "example": "satellite.console.display(satellite.system.memory.this.available(\"kb\") > 0)"
 },
 "1 22 4 5 2": {
  "query": "satellite.help(satellite.system.memory.this.free)",
  "prose": "How much room this thread still has on its stack — the same\nquestion `available` asks, because a word means one thing everywhere.",
  "example": "satellite.console.display(satellite.system.memory.this.free(\"kb\") > 0)"
 },
 "1 22 4 5 3": {
  "query": "satellite.help(satellite.system.memory.this.used)",
  "prose": "How much of its stack this thread is using.",
  "example": "satellite.console.display(satellite.system.memory.this.used(\"kb\") > 0)"
 },
 "1 22 4 5 4": {
  "query": "satellite.help(satellite.system.memory.this.available)",
  "prose": "How much room this thread still has, in the unit you name.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.this.available(\"kb\") > 0)"
 },
 "1 22 4 5 5": {
  "query": "satellite.help(satellite.system.memory.this.free)",
  "prose": "How much room this thread still has, in the unit you name — the\nsame question `available(unit)` asks.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.this.free(\"kb\") > 0)"
 },
 "1 22 4 6": {
  "query": "satellite.help(satellite.system.memory.free)",
  "prose": "How much of the machine's memory is unused.\n\n**Unused means AVAILABLE and not free.** Linux spends every spare page\non cache, so the \"free\" figure on a healthy machine is a small\nfrightening number; available is what a program can still have.",
  "example": "satellite.console.display(satellite.system.memory.free(\"mb\") > 0)"
 },
 "1 22 4 7": {
  "query": "satellite.help(satellite.system.memory.total)",
  "prose": "How much memory the machine has altogether.",
  "example": "satellite.console.display(satellite.system.memory.total(\"gb\") > 0)"
 },
 "1 22 4 8": {
  "query": "satellite.help(satellite.system.memory.used)",
  "prose": "How much of the machine's memory is in use — its total less what\nis available.",
  "example": "satellite.console.display(satellite.system.memory.used(\"mb\") > 0)"
 },
 "1 22 4 10": {
  "query": "satellite.help(satellite.system.memory.free)",
  "prose": "How much of the machine's memory is unused, in the unit you name.\n\n**Unused means AVAILABLE and not free.** Linux spends every spare page\non cache, so the \"free\" figure on a healthy machine is a small\nfrightening number; available is what a program can still have.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.free(\"gb\") > 0)"
 },
 "1 22 4 11": {
  "query": "satellite.help(satellite.system.memory.total)",
  "prose": "How much memory the machine has altogether, in the unit you name.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.total(\"gb\") > 0)"
 },
 "1 22 4 12": {
  "query": "satellite.help(satellite.system.memory.used)",
  "prose": "How much of the machine's memory is in use, in the unit you name.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.used(\"gb\") > 0)"
 },
 "1 22 5": {
  "query": "satellite.help(satellite.system.threshold)",
  "prose": "Answers how loose a search is allowed to be at the moment.\n\nThe scale runs from 1, which is an exact match and nothing else, up to\n10. At 6 a substring matches either way round.",
  "example": "satellite.console.display(satellite.system.threshold())"
 },
 "1 22 6": {
  "query": "satellite.help(satellite.system.threshold)",
  "prose": "Sets how loose a search may be, and answers what it now is.\n\nIt is what `search` on a list or a map reads, so turn it up to find near\nmisses and down to insist on the exact thing.",
  "example": "satellite.system.threshold(6)\nsatellite.console.display(satellite.system.threshold())"
 },
 "1 22 7": {
  "query": "satellite.help(satellite.system.persist)",
  "prose": "Answers whether the prompt is keeping what each line declares.\n\nIt is on to begin with, which is why a variable you declare at the prompt\nis still there on the next line.",
  "example": "satellite.console.display(satellite.system.persist())"
 },
 "1 22 8": {
  "query": "satellite.help(satellite.system.persist)",
  "prose": "Turns the prompt's memory of declarations on or off, and answers what it\nnow is.\n\nTurning it off does not throw away what is already held; it stops the next\nline adding to it, so a program can switch it off around one line and back\non afterwards. It wants a real bool, so write\n`satellite.bool.false` rather than a bare word.",
  "example": "satellite.console.display(satellite.system.persist(satellite.bool.false))\nsatellite.console.display(satellite.system.persist(satellite.bool.true))"
 },
 "1 22 4 4 7": {
  "query": "satellite.help(satellite.system.memory.swap.used)",
  "prose": "How much swap is in use, in the unit you name.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.swap.used(\"kb\") >= 0)"
 },
 "1 22 4 5 6": {
  "query": "satellite.help(satellite.system.memory.this.used)",
  "prose": "How much of its stack this thread is using, in the unit you name.\n\nThe unit is `\"b\"`, `\"kb\"`, `\"mb\"`, `\"gb\"` or `\"tb\"`, and `\"mb\"` is\nwhat you get without asking. Every one of them is a power of 1024 away\nfrom a byte, so the answer is exact and never rounds.",
  "example": "satellite.console.display(satellite.system.memory.this.used(\"kb\") > 0)"
 },
 "1 22 9": {
  "query": "satellite.help(satellite.system.environment)",
  "prose": "One environment variable, by name.\n\nWritten bare, `satellite.system.environment` answers a map of the whole\nenvironment; given a name it answers just that one.\n\n**A name that is not set answers nothing, not an empty string.** An empty\nstring is a value a variable can really hold, so the two must stay apart.",
  "example": "satellite.console.display(satellite.system.environment(\"HOME\"))\nsatellite.console.display(satellite.system.environment(\"NO_SUCH_VARIABLE\"))"
 }
}